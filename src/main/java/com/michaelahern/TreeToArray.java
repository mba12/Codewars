package com.michaelahern;

import java.util.ArrayList;
import java.util.List;

public class TreeToArray {
/*
    List<Integer> list = new ArrayList<>();
    public TreeNode createTreeNode(int val) {
        return new TreeNode(val);
    }

    TreeNode nodeAndChildren(int val, TreeNode left, TreeNode right) {
        TreeNode node = new TreeNode(val);
        node.left = left;
        node.right = right;
        return node;
    }

    public TreeNode TreeToArray(TreeNode root) {

        return arrayToTree(root, 0);
    }

    TreeNode arrayToTree(TreeNode node, int index) {
        if (index >= array.length)
            return null;
        if (node == null) {
            list.set(index, null);
            return null;
        }
            return null;
        return new TreeNode(list.set(index, node.val),
                            arrayToTree(list, array, index * 2 + 1), arrayToTree(array, index * 2 + 2));
    }
*/

}
