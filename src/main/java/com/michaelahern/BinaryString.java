package com.michaelahern;

public class BinaryString {

    // NOTE: not complete
    public String toBinaryString(int n) {

        String nStr = n + "";
        int len = nStr.length();

        int digits = (int) Math.pow(2, len - 1);

        return "";

        


    }
}
